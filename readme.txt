Для запуска программы из командной строки Windows:
1. Установить Java SE Development Kit
2. Добавить в значение переменной среды PATH путь к файлам javac.exe и java.exe
3. Скопировать проект в любую папку
4. В папку src\ добавить файлы необходимые для сортировки слиянием
5. В командной строке Windows переместится в папку src, "cd <диск>\<путь к проекту>\src
6. Скомпилировать проект, ввести в командной строке: "javaс ru\cft\khanov\Main.java"
7.1. В случае появления сообщения: "не является внутренней или внешней командой..."
перейти на шаг 2 и проверить правильность пути к файлам javac.exe и java.exe
7.2. В случае появления сообщения: "Note: Some input files use unchecked or unsafe operations..." - перейти на шаг 6
8. Запустить проект, ввести в командной строке:
 "java ru.cft.khanov.Main <ключ параметр сортировки> <ключ тип данных> <имя результирующего файла> <входной файл 1>...<входной файл N>"

********** Особенности реализайии **********
1. Алгоритм реализован для целых чисел с диапазонами:
-параметр -i: от -2147483648 до 2147483647 (int).
-параметр -l: от -9223372036854775808 до 9223372036854775807 (long).
-параметр -b: от -2^Integer.MAX_VALUE до 2^Integer.MAX_VALUE (BigInteger).
2. В случае сортировки целых чисел, при попадании числа вне диапазона, сортировка продолжается,
появляется сообщение с названием файла, где содержатся не корректные данные и содержимое не корректной строки.
3. В случае сортировки целых чисел, при попадании строк содержащие символьные значения, сортировка продолжается,
появляется сообщение с названием файла, где содержится не корректные данные и содержимое не корректной строки.
4. При сортировке строк, в случае попадания строк содержащих цифры, сортировка продолжается,
появляется сообщение с названием файла, где содержится не корректные данные и содержимое не корректной строки.
5. Пустые строки пропускаются

!!! Успешно проведено тестирование методом атомарных проверок (34 теста), см. файл Результат тестирования атомарными проверками.xlsx 

Параметры программы задаются при запуске через аргументы командной строки, по порядку:
1. режим сортировки (-a или -d), необязательный, по умолчанию сортируем по возрастанию;
2. тип данных (-s, -i, -l или -b), обязательный;
3. имя выходного файла, обязательное;
4. остальные параметры – имена входных файлов, не менее одного.
Примеры запуска из командной строки для Windows:
java ru.cft.khanov.Main -i -a out.txt in.txt (для целых чисел (int) по возрастанию)
java ru.cft.khanov.Main -s out.txt in1.txt in2.txt in3.txt (для строк по возрастанию)
java ru.cft.khanov.Main -d -s out.txt in1.txt in2.txt (для строк по убыванию)
